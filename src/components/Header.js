import React from "react";

const Header = () => {
  return (
    <div>
      <nav
        className="navbar navbar-dark bg-dark"
        style={{ color: `white`, justifyContent: `center`, height: `4rem` }}
      >
        NGIT d.o.o. - zadatak za kandidate
      </nav>
    </div>
  );
};
export default Header;
